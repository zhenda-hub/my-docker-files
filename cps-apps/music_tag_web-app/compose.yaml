version: '3'

services:
  music-tag:
    image: xhongc/music_tag_web:latest
    container_name: music-tag-web
    ports:
      - "8002:8002"
    volumes:
      - ./music:/app/media:rw
      - ./config:/app/data
    restart: unless-stopped